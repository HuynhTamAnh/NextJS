import React from "react";

const Button = () => {
  return (
    <div>
      <button>thêm mới</button>
      <button>xoá</button>
      <button>sửa</button>
    </div>
  );
};

export default Button;
